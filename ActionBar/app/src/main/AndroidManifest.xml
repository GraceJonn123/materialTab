<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jijjy.grace.actionbar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuInScrollViewActivity" />
        <activity android:name=".MenuWIthCustomActionButtonActivity" />
        <activity android:name=".MenuWIthCustomAnimationActivity" />
        <activity android:name=".MenuWithFABActivity" />
        <activity android:name=".SlideInAnimationHandler" />
        <activity android:name=".SystemOverlayMenuActivity" />
        <activity android:name=".SystemOverlayMenuService"></activity>
    </application>

</manifest>
